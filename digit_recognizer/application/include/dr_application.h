#ifndef DR_APPLICATION_H
#define DR_APPLICATION_H

typedef struct {
} dr_application;

//dr_application* dr_application_create(const size_t width, const size_t height, const char* title);
//void dr_application_free(dr_application* application);

#endif // DR_APPLICATION_H